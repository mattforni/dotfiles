# Permissions Learned Rules

Corrections and calibration for how `assist:permissions` should reason about what to hoist, what to collapse into a wildcard, and what stays at project level. Overrides SKILL.md. Read on every invocation.

_(Empty. Populated as Forni corrects permissions' judgment over time.)_
